enum ApiErrorType {
  cancelled,
  connection,
  timeout,
  badCertificate,
  badRequest,
  unauthorized,
  forbidden,
  notFound,
  methodNotAllowed,
  conflict,
  unsupportedMediaType,
  validation,
  tooManyRequests,
  client,
  server,
  parsing,
  unknown,
}

enum FirebaseErrorType {
  cancelled,
  network,
  unauthenticated,
  permissionDenied,
  notFound,
  alreadyExists,
  invalidArgument,
  invalidCredential,
  userDisabled,
  userNotFound,
  emailAlreadyInUse,
  weakPassword,
  tooManyRequests,
  quotaExceeded,
  unavailable,
  requiresRecentLogin,
  operationNotAllowed,
  unknown,
}
